@model IEnumerable<ParkingLotWebApp.Models.ParkingLotsDetail>

@{
    ViewBag.Title = WebPages.ParkingLotAreas_Index;
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink(WebPages.ParkingLotAreas_Create_New, "Create")
    
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th> 
        <th>
            @Html.DisplayNameFor(model => model.LastUpdate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
     
        <td>
            @Html.DisplayFor(modelItem => item.LastUpdate)
        </td>
        <td>
            @Html.ActionLink(Common.Edit, "Edit", new { id=item.ID }) |
            @Html.ActionLink(Common.Details, "Details", new { id = item.ID }) |
            @Html.ActionLink(Common.Delete, "Delete", new { id=item.ID }) |
            @Html.ActionLink(WebPages.ParkingLotAreas_FloorsLink, "Index", "ParkingLotsFloor", new { id = item.ID, returnUrl = Url.Action("Index", "ParkingLotAreas", null) }, null)
        </td>
    </tr>
}

</table>
